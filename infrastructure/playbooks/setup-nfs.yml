---
- name: Setup NFS Mounts
  hosts: k3s_cluster
  become: true
  gather_facts: true
  roles:
    - role: nfs
      when: 
        - nfs_enabled | default(false) | bool
        - nfs_server is defined and nfs_server | length > 0
